# Public — safe to bake into HTML and expose to the browser
X_PUBLIC_APP_NAME=My Site
X_PUBLIC_API_URL=https://api.example.com

# Server-only — never leaves the server
DATABASE_URL=postgres://localhost:5432/mydb
API_SECRET=super_secret_key
JWT_SECRET=very_secret